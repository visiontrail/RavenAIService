## 1. Backend: user language + locale resolution

- [x] 1.1 Add `language` column (VARCHAR(8), NOT NULL, default `zh`) to `User` in `app/models/user.py`
- [x] 1.2 Create Alembic migration under `alembic/versions/` adding the `language` column with default `zh`
- [x] 1.3 Define a shared supported-locale constant + validator (e.g. `app/i18n/__init__.py` with `SUPPORTED = ("zh","en")`, `DEFAULT = "zh"`, `normalize(code)->code`)
- [x] 1.4 Expose `language` in the user profile read response and `UserProfile` schema
- [x] 1.5 Add `language` to the profile-update endpoint, coercing/rejecting unsupported codes via the validator
- [x] 1.6 Add a request-scoped FastAPI dependency resolving locale: locale header → `current_user.language` → default `zh`

## 2. Backend: localized server-generated text

- [x] 2.1 Create a backend message catalog (`app/i18n/messages.py`, dict keyed by locale + message id) and a `t(key, locale, **fmt)` helper with `zh` fallback
- [x] 2.2 Migrate user-facing API success/error/validation strings to catalog keys (start with `app/utils/file_upload_validator.py`, `app/api/*.py` user-facing messages) — _`app/api/logs.py` now fully localized: all user-facing `message=`/`detail=` strings in all endpoints (upload, list, get, delete, download, batch ops, AI analysis, status, SSE trace stream, issue description, manual analysis) route through `t(key, locale)` via `get_request_locale` dependency; the manual `resolve_locale` call in `trigger_ai_analysis` replaced by the dependency (locale still passed to Celery task); `_ERROR_KIND_MESSAGES` dict replaced with catalog-keyed `t(f"log.error_kind.{error_kind}", locale)`. New `log.*` keys added (zh+en) to `app/i18n/messages.py`; parity holds. **`app/api/admin.py` skipped** — admin-UI only, messages are internal/English; noted as not user-facing per spec._
- [x] 2.3 Ensure background/Celery tasks resolve the relevant owner's `language` at enqueue time and pass it to `t()` instead of relying on a request — _`run_ai_analysis_task` already accepts `locale` (resolved at enqueue in the endpoint via `get_request_locale`) and threads it to `workspace_ctx.locale`. Other Celery tasks (`log_processing.py`, `bug_fix.py`, `cleanup_tasks.py`) produce no user-facing text via `t()`. Verified `missing_keys()` == {} — no catalog gaps._

## 3. Prompts: per-language structure

- [x] 3.1 Restructure `app/prompts/prompts_config.yaml` so each user-facing prompt body (`system_prompt`, `user_prompt_template`, chat `template`) becomes a `{zh, en}` map; move current Chinese bodies into `zh` verbatim — _done: all prompt bodies restructured to `{zh, en}` maps. Existing Chinese content moved verbatim into `zh:` keys. YAML loads correctly; `prompts_config_service` and agent loaders select per-language with `zh` fallback; all 22 `test_i18n_prompts.py` tests pass._
- [x] 3.2 Author `en` bodies for log-analysis, device, project-expert, package-search, general agents, and the chat session-title prompt — _done together with 3.1: all English bodies authored for `chat.session_title_prompt`, `claude_agent_log_analysis.generic`, `claude_agent_project_expert.generic`, and `claude_agent_device.default`. `package_search` and `general_agent` use module-level constants (no `prompts_config.yaml` entries); their language is handled by the response-language directive (4.3)._
- [x] 3.3 Update each agent `prompts.py` (`log_analysis`, `device_agent`, `project_expert`, `package_search`, `general_agent`) `get_prompts()` to take a `locale` arg and select the body with `zh` fallback — _done via shared `app/i18n/prompts.py:select_localized_body`; `log_analysis`/`project_expert`/`device_agent` `get_prompts(..., locale=)` and `package_search` `get_system_prompt(locale=)` all select per-language with `zh` fallback and stay backward-compatible with flat strings. `general_agent` keeps its module-level constant (no `prompts.py`/`get_prompts`); its language is handled by the 4.3 directive + a future per-language constant_
- [x] 3.4 Update `app/services/prompts_config_service.py` to read/write the nested per-language bodies and keep cache invalidation on save — _`_extract_prompt_entries` now emits one editable entry per language variant (id/path include the locale, e.g. `...system_prompt.zh`); `_path_set` allows authoring a new variant under an existing per-language map; `get_chat_title_prompt_template(locale=)` and `get_device_agent_prompts(..., locale=)` select per-language; existing cache invalidation on save unchanged_

## 4. AI: locale threading + response-language directive

- [x] 4.1 Thread the resolved locale from each agent's API/task entry point into its agent run and into `get_prompts(locale=...)` — _log-analysis fully threaded on both paths (async task + chat). **Now also threaded:** (a) `package_search` — `POST /packages/agent-search` resolves locale (header → Accept-Language → user) and passes it to `PackageSearchAgent.run/stream(locale=)` → `_drive` selects `get_system_prompt(locale)` + appends the response-language directive; (b) `project_expert` — `POST /project-expert/stream` resolves locale into `project_expert_chat_service.stream(locale=)`, which sets `WorkspaceContext.locale`; `ProjectExpertAgent.run` calls `get_prompts(locale=ctx.locale)` + appends the directive; (c) `device_agent` + `general_agent` — `POST /chat` and `POST /chat/stream` resolve locale into `ai_chat_service.chat(locale=)` / `chat_run_service.start_device_run(locale=)` / `start_general_run(locale=)`, which set `DeviceAgentContext.locale` / `GeneralAgentContext.locale`; both agents append the directive (device also passes locale to `get_prompts`). `general_agent` keeps its single `SYSTEM_PROMPT` constant; only the directive varies by language until a per-language body is authored._
- [x] 4.2 Thread locale into chat session-title generation so titles are produced in the active language — _resolved request locale flows `ai_chat.py` → `stream(locale=)` → `AgentJob.locale` → `_persist_exchange(locale=)` → `ai_chat_service.generate_session_title(locale=)` → `title_generator_service.generate_session_title(locale=)` → `get_chat_title_prompt_template(locale)`._
- [x] 4.3 Build a response-language directive helper (`zh`/`en`) and append it to each agent's system prompt so the model answers in the active language regardless of input-data language — _`app/i18n/prompts.py:response_language_directive(locale)` returns a blunt imperative directive (`zh`/`en`, `zh` fallback). Appended **last** to the log-analysis system prompt right before `build_options`. Other agents adopt it as their entry points are threaded in 4.1._
- [x] 4.4 Verify async log-analysis tasks run with the log owner's language end to end — _path is wired end to end (endpoint resolves locale at enqueue → `run_ai_analysis_task(locale)` → `workspace_ctx.locale = normalize(locale)` at `app/tasks/ai_analysis.py:836` → `get_prompts(locale)` + directive). Verification test added: `tests/test_log_analysis_agent.py::TestLogAnalysisAgentRun::test_run_threads_locale_into_prompts_and_directive` (parametrized `en`/`zh`) asserts `ctx.locale` flows into `get_prompts(locale=...)` and that the matching `response_language_directive(locale)` is appended last to the system prompt handed to `build_options`, with the other locale's directive absent._

## 5. Frontend: i18n framework

- [x] 5.1 Add `vue-i18n` dependency to `frontend/package.json`
- [x] 5.2 Create `frontend/src/i18n/` with `zh.ts`, `en.ts`, and `index.ts` (supported-locale list + configured i18n instance) — _`index.ts` is the single source of truth: `SUPPORTED_LOCALES`, `DEFAULT_LOCALE='zh'`, `normalizeLocale`/`detectBrowserLocale`, the configured `i18n` instance (legacy:false, datetime/number formats), Element Plus locale packs (`getElementLocale`), `getActiveLocale`/`setI18nLocale`, and `LOCALE_HEADER='X-App-Locale'`/`LOCALE_STORAGE_KEY`. Catalogs seeded with `common`/`language` namespaces; view strings extracted in 6.x_
- [x] 5.3 Register i18n in `main.ts`; wire Element Plus `el` locale packs and date/number formats to the active locale — _`main.ts` registers `i18n` and seeds Element Plus with `getElementLocale()`; reactive switching handled by an `ElConfigProvider` in `App.vue` bound to `getElementLocale(appStore.locale)`; datetime/number formats defined per locale on the i18n instance_
- [x] 5.4 Add `locale` state + setter to the `app.ts` store; boot resolution = profile (if authed) → `localStorage` → `navigator.language` detection (`en*`→`en`, else `zh`) — _`app.ts` holds reactive `locale` (init from `getActiveLocale()` = localStorage→browser); `initLocale()` prefers `userStore.profile.language` when authenticated; called after user bootstrap in `WorkbenchLayout.vue`_
- [x] 5.5 Persist on switch: update store + `localStorage`, set vue-i18n/Element Plus locale, and PATCH profile when authenticated; on login prefer profile over `localStorage` — _`appStore.setLocale(next, {persistProfile})` sets i18n+localStorage and PATCHes `/users/auth/me` when authed (via new `userApi.updateProfile`); login calls `initLocale()` (profile wins); register persists the anonymous choice to the new profile_
- [x] 5.6 Make the API client (`frontend/src/api/index.ts`) send the active locale header on every request — _shared `localeHeaderInterceptor` exported from `api/index.ts` and attached to every axios instance (`api`, `userClient`, `adminClient`, `chatApi`, releases public/admin clients), sending `X-App-Locale: <active locale>`. DOM-free locale primitives live in `i18n/runtime.ts` (no vue-i18n import) so the axios layer reads `getActiveLocale()` without pulling `@vue/runtime-dom` into node-env tests_

## 6. Frontend: string extraction + switcher

- [x] 6.1 Add a language switcher control in the layout (`frontend/src/layouts/`) — _wired the existing (previously dead, local-`lang`-ref) language pill in the user menu of `WorkbenchLayout.vue` to the app store: `setLanguage(next)` calls `appStore.setLocale` (store + localStorage + vue-i18n/Element Plus, PATCHes profile when authed), `activeLocale` computed drives the active state + the `lang-en` body class, and the `语言` label now reads from `t('language.label')`. Test harness installs the `i18n` plugin so the layout spec keeps passing_
- [ ] 6.2 Extract hardcoded strings into catalogs view-by-view: `LogList.vue`, `LogDetail.vue`, `AIChat.vue`, `Upload.vue`, `Download.vue`, device views, Raven views — _**done**: `LogList.vue` (full — template + JS, `logList.*` keys), `Upload.vue` (full — `upload.*` keys), `Download.vue` (full — `download.*` keys), `NotFound.vue` (full — `notFound.*` keys, task 6.4 scope), `DeviceList.vue` + `DeviceDetail.vue` (full — `device.*` keys), `RavenPackageDetail.vue` (full — `raven.*` keys added, `RavenManager.vue` pending). **Still pending**: `LogDetail.vue` (2699 lines), `AIChat.vue` (2502 lines), `RavenManager.vue` (1811 lines)._
- [ ] 6.3 Extract strings in admin views (`AdminPrompts.vue`, `AdminUsers.vue`, `AdminMetrics.vue`, `AdminModelSettings.vue`, `AdminProjectRepos.vue`, `AdminProjectSkills.vue`, `AdminAgentSkills.vue`, `AdminRelease.vue`)
- [ ] 6.4 Extract strings in shared `components/` and `NotFound.vue`
- [ ] 6.5 Update `AdminPrompts.vue` to view/edit each prompt's `zh` and `en` bodies independently

## 7. Validation & tests

- [ ] 7.1 Add a catalog-parity check (every key exists in both `zh` and `en`) for frontend catalogs and the backend message catalog
- [ ] 7.2 Add a CJK-literal scan over `.vue`/`.ts` (outside `i18n/`) to catch leftover hardcoded strings
- [x] 7.3 Update existing prompt/agent tests (`tests/test_log_analysis_agent.py`, etc.) for the new per-language prompt structure and `locale` argument — _existing prompt/agent suites are backward-compatible with the new optional-`locale` loaders and pass unchanged (`tests/agents/**` 91 passed; `get_prompts()` without a locale still returns the `zh`/legacy body). New per-language loader coverage lives in `tests/test_i18n_prompts.py` (log-analysis/project-expert/device/package-search loaders select locale with `zh` fallback). Added `test_run_threads_locale_into_prompts_and_directive` to `tests/test_log_analysis_agent.py` for the `locale` argument path._
- [x] 7.4 Add tests: locale resolution chain (header → user → default), profile language validation, prompt language selection + fallback, and the response-language directive — _all four covered: locale resolution chain → `tests/test_i18n_locale.py` (`resolve_locale` header > user > default, Accept-Language, unsupported→default); profile language validation → `tests/api/test_user_profile_language.py`; prompt language selection + fallback → `tests/test_i18n_prompts.py` (`select_localized_body` + loaders); response-language directive → new `test_response_language_directive_*` in `tests/test_i18n_prompts.py` (locale selection, loose-locale normalization, default-`zh` fallback)._
- [ ] 7.5 Manual verification: English UI end to end, and an English answer produced over Chinese-language log input
