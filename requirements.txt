fastapi>=0.108.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6
python-dotenv>=1.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.0
psutil>=5.9.7
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.19.0

# Celery and Redis for async task processing
celery>=5.3.0
redis>=5.0.0
kombu>=5.3.0

# Additional utilities for file processing

# Agent framework and LLM clients
claude-agent-sdk>=0.1
langchain>=0.2.15
langchain-community>=0.2.15
langchain-openai>=0.1.7
langgraph>=0.1.18
openai>=1.40.0
tiktoken>=0.7.0

# YAML parser for prompts_config.yaml
PyYAML>=6.0.1

# Prometheus metrics surface (soft-imported; the app degrades to no-op
# counters/gauges if this is missing in older deployment images).
prometheus-client>=0.20.0
